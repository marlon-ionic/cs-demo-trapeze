import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cs-demo-trapeze',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
